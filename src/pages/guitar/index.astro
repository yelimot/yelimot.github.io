---
import BaseLayout from '../../layouts/BaseLayout.astro';
import BlogPostCard from '../../components/BlogPostCard.astro';
import { getCollection } from 'astro:content';

const posts = (await getCollection('guitar')).sort(
  (a, b) => new Date(b.data.date).getTime() - new Date(a.data.date).getTime()
);
---

<BaseLayout title="Guitar" description="Guitar blog posts by Yasar Polatli">
  <section class="space-y-2">
    <h1 class="font-display text-3xl font-bold">Guitar</h1>
    <p class="text-[var(--color-text-secondary)] text-sm mb-6">
      Notes on music, songs, and playing.
    </p>
    {posts.length === 0 ? (
      <p class="text-[var(--color-text-secondary)]">No posts yet.</p>
    ) : (
      posts.map(post => (
        <BlogPostCard
          title={post.data.title}
          description={post.data.description}
          href={`/guitar/${post.id}`}
          date={post.data.date}
        />
      ))
    )}
  </section>
</BaseLayout>
